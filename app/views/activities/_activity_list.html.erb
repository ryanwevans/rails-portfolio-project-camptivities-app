<% if camp %>
  <p style="text-align:center;"><%= link_to "Add New Activity", new_camp_activity_path(@camp) %>
  </p>
  <br>
  <% camp.activities.each do |activity| %>
    <p>
      <%= link_to activity.name, activity_path(activity) %>
      <br>
      <br>
      <%= activity.description %>
      <br>
      Fun Rating: <%= activity.assignments.average(:rating) %>
      <br>
      <%= activity.comment.last %>
      <br>
    </p>
    <%= render partial: 'activities/activity_availability', locals: {assignments: activity.assignments} %>
    <br>
    <br>
  <% end %>
  <br>
  <br>
<% else %>
  <p style="text-align:center;"><%= link_to "Add New Activity", new_activity_path %>
  </p>
  <br>
  <% camps.each do |camp| %>
    <h3 style="font-weight:500;"><%= camp.name %></h3>
    <% camp.activities.each do |activity| %>
      <p>
        <strong>
          <%= link_to "#{activity.name}", activity_path(activity) %>
        </strong>
        <br>
        <br>
        <%= activity.description %>
        <br>Fun Rating: <%= activity.assignments.average(:rating) %>
        <br>
        <%= activity.comment.last %>
        <br>
      </p>
    <% end %>
    <br>
  <% end %>
<% end %>
